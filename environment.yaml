name: genforce
channels:
  - pytorch
  - defaults
dependencies:
  - python=3.10
  - pip
  - numpy
  - pip:
      - torch==2.7.0
      - torchvision==0.22.0
      - torchmetrics
      - torchdata
      - opencv-python
      - einops
      - open-clip-torch
      - clip @ git+https://github.com/openai/CLIP.git
      - transformers
      - urllib3
      - xformers==0.0.30
      - streamlit-keyup
      - diffusers
      - gradio
      - wandb
      - vision-aided-loss
      - clean-fid
      - dominate
      - lpips
      - peft
      - tokenizers
      - tqdm
      - taichi
      - pytransform3d
      - imageio
      - open3d
      - scikit-image
      - pillow
      - scipy
      - timm
